#include <stdio.h>
#include "net.h"
#include "queue.h"

void rcv(queue_t *rcv_q) {
  // a function to receive moves from other players, probably using a queue
  return;
}

void send(queue_t *send_q) {
  // a function to send moves to other players, probably with a queue
  return;
}

int main(void) {
  printf("Hello from network\n");
}
